server.port=${PORT:8080}
spring.application.name=shop-api

# MongoDB Atlas
spring.data.mongodb.uri=${MONGODB_URI:mongodb+srv://admin:admin@cdweb.e6j1cx9.mongodb.net/shopdb?retryWrites=true&w=majority&appName=cdweb}

jwt.secret=${JWT_SECRET:cdwebShopSecretKey2026MustBeAtLeast256BitsLongForHS256Algorithm}
jwt.expiration=${JWT_EXPIRATION:86400000}

# Clerk OAuth2 Resource Server
spring.security.oauth2.resourceserver.jwt.jwk-set-uri=https://new-dove-6.clerk.accounts.dev/.well-known/jwks.json

app.cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:3000}
